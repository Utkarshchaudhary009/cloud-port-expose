# Copy to .env.cloud-expose and fill in your values.
# NEVER commit .env.cloud-expose — keep credentials out of git and URLs.
CLOUD_EXPOSE_RELAY=wss://your-relay.example.com/___agent

# Client credential issued by your relay/control plane.
# Leave empty only if your self-hosted relay runs without an auth store.
CLOUD_EXPOSE_TOKEN=
